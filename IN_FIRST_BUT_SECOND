#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
	//code
	 int t;
	 cin>>t;
	 while(t--)
	  {
	      int n1,n2;
	      cin>>n1>>n2;
	      unsigned long long int a[n1],k;
	      unordered_set<long long int>s;
	          int i,j;
	         for(i=0;i<n1;i++)
	           cin>>a[i];
	         for(i=0;i<n2;i++)
	          {
	              cin>>k;
	              s.insert(k);
	          }
	       for(i=0;i<n1;i++)
	        if(s.find(a[i])==s.end())
	         cout<<a[i]<<" ";
     	      cout<<endl;
	  }
	return 0;
}
